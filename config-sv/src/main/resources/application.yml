server:
  port: 8095

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/francodavyd/Microservices-application.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}